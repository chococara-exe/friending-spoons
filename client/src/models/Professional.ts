export default interface Professional {
    type: "Friend" | "Spooner",
    name: string,
    age: number,
    email: string,
    image: string,
    tags: string[],
    location: string,
    description: string
}